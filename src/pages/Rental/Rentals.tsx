import SideBar from "../../components/Sidebar/SideBar";

type Props = {};

const Rentals = (props: Props) => {
  return (
    <SideBar>
      <div></div>
    </SideBar>
  );
};

export default Rentals;
